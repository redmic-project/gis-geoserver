include:
  - project: 'redmic-project/gitlab-ci-templates'
    ref: master
    file: '/deployment.yml'

stages:
  - deploy

.deploy:
  variables:
    STACK: gis
  environment:
    url: https://${DD_GEOSERVER_SUBDOMAIN}.${PUBLIC_HOSTNAME}
